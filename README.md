# BookStore Website

An implementation of a MVC (Model-View-Controller) architecture on a bookstore website.

The code is written in .NET Core, MSSQL server and Entity Framework Core.

The application allows users to browse through a collection of books, create new books. Users can also register and log in to the application. The database schema 

includes tables for users, books, orders, and order items.
